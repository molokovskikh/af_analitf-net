директория содержит в себе данные
что бы воссоздать определенные условия в приложение
которые нуждаются в тестирование

пример
фильтр адресов появляется только если адресов больше одноного
для этого есть create_address.sql

для применения
./scripts/run-mysql-client.sh --execute "`cat ./fixtures/create_address.sql`"
