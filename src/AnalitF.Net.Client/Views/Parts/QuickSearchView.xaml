<UserControl x:Class="AnalitF.Net.Client.Views.Parts.QuickSearchView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<TextBox Style="{StaticResource PopupSearch}"
		x:Name="SearchText"
		Visibility="{Binding SearchTextVisible, Converter={StaticResource VisibilityConverter}}"
		IsEnabled="{Binding IsEnabled}"></TextBox>
</UserControl>
