<UserControl x:Class="AnalitF.Net.Client.Views.MnnView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:AnalitF.Net.Client.Controls"
	xmlns:binders="clr-namespace:AnalitF.Net.Client.Binders"
	binders:FocusBehavior.DefaultFocus="{x:Reference Mnns}">

	<UserControl.InputBindings>
		<KeyBinding Gesture="ESCAPE" Command="binders:Commands.InvokeViewModel" CommandParameter="NavigateBackward" />
	</UserControl.InputBindings>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition></RowDefinition>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Style="{StaticResource ResourceKey=FilterPanel}">
			<TextBox x:Name="SearchText" Style="{StaticResource ResourceKey=SearchText}" binders:SearchBinder.Grid="Mnns"></TextBox>
			<CheckBox Name="ShowWithoutOffers" Style="{StaticResource ResourceKey=FilterCheckBox}">Отображать весь каталог</CheckBox>
		</StackPanel>
		<c:DataGrid Grid.Row="1" x:Name="Mnns" c:SearchableDataGridColumn.SearchTerm="{Binding SearchText}">
			<DataGrid.Columns>
				<c:SearchableDataGridColumn
					CellStyle="{StaticResource ResourceKey=HaveOffers}"
					HighlightStyle="{StaticResource DataGridHighlight}"
					Width="*" Header="МНН" Binding="{Binding Name}" />
			</DataGrid.Columns>
		</c:DataGrid>
	</Grid>
</UserControl>
