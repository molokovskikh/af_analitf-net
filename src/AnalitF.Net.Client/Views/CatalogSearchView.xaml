<UserControl x:Class="AnalitF.Net.Client.Views.CatalogSearchView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:AnalitF.Net.Client.Controls"
	xmlns:Parts="clr-namespace:AnalitF.Net.Client.Views.Parts"
	xmlns:cal="http://www.caliburnproject.org"
	xmlns:Binders="clr-namespace:AnalitF.Net.Client.Binders"
	Binders:FocusBehavior.DefaultFocus="{x:Reference Catalogs}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<StackPanel Style="{StaticResource ResourceKey=FilterPanel}" Grid.Row="0">
			<TextBox x:Name="SearchText" Style="{StaticResource ResourceKey=SearchText}"
				Binders:SearchBinder.Grid="Catalogs" />
			<CheckBox x:Name="QuickSearch_IsEnabled" Style="{StaticResource FilterCheckBoxSmall}">Искать в начале слова</CheckBox>
		</StackPanel>

		<c:DataGrid Grid.Row="1" x:Name="Catalogs"
			c:SearchableDataGridColumn.SearchTerm="{Binding ActiveSearchTerm}">
			<DataGrid.Columns>
				<c:SearchableDataGridColumn
					CellStyle="{StaticResource ResourceKey=CatalogHaveOffers}" Width="*"
					Header="Наименование"
					Binding="{Binding Name}"
					HighlightStyle="{StaticResource DataGridHighlight}" />
				<c:SearchableDataGridColumn
					CellStyle="{StaticResource ResourceKey=CatalogHaveOffers}" Width="*"
					Header="Формы выпуска"
					Binding="{Binding Form}"
					HighlightStyle="{StaticResource DataGridHighlight}" />
			</DataGrid.Columns>
		</c:DataGrid>

		<ContentControl x:Name="QuickSearch" Grid.Row="1" />
		<Parts:CatalogPanel Grid.Row="2" cal:Bind.ModelWithoutContext="{Binding}"
			DataContext="{Binding ParentModel}" />
	</Grid>
</UserControl>
