<UserControl x:Class="AnalitF.Net.Client.Views.PriceView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:c="clr-namespace:AnalitF.Net.Client.Controls" mc:Ignorable="d"
	xmlns:binders="clr-namespace:AnalitF.Net.Client.Binders"
	binders:FocusBehavior.DefaultFocus="{x:Reference Prices}">

	<UserControl.InputBindings>
		<KeyBinding Gesture="ESCAPE" Command="binders:Commands.InvokeViewModel" CommandParameter="NavigateBackward" />
	</UserControl.InputBindings>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition></ColumnDefinition>
			<ColumnDefinition Width="200"></ColumnDefinition>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition></RowDefinition>
			<RowDefinition Height="Auto"></RowDefinition>
		</Grid.RowDefinitions>
		<StackPanel Style="{StaticResource ResourceKey=FilterPanel}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
			<TextBlock Margin="5">
				Всего прайс листов: <Run x:Name="Prices_Count"></Run>
			</TextBlock>
		</StackPanel>
		<c:DataGrid Grid.Row="1" Grid.Column="0" x:Name="Prices">
			<DataGrid.Columns>
				<DataGridTextColumn CellStyle="{StaticResource ResourceKey=NotBasePrice}" Width="*" Header="Прайс-лист" Binding="{Binding Name}" />
				<DataGridTextColumn Width="*" Header="Регион" Binding="{Binding RegionName}" />
				<DataGridCheckBoxColumn Width="*" Header="Склад" Binding="{Binding Storage}" />
				<DataGridCheckBoxColumn Width="*" Header="В работе" />
				<DataGridTextColumn Width="*" Header="Позиций" Binding="{Binding PositionCount}" />
				<DataGridTextColumn Width="*" Header="Заказ" />
				<DataGridTextColumn Width="*" Header="Сумма" />
				<DataGridTextColumn Width="*" Header="Дата прайс-листа" Binding="{Binding PriceDate}" />
				<DataGridTextColumn Width="*" Header="Месячный заказ" />
				<DataGridTextColumn Width="*" Header="Недельный заказ" />
			</DataGrid.Columns>
		</c:DataGrid>
		<Grid Grid.Row="1" Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"></RowDefinition>
				<RowDefinition Height="Auto"></RowDefinition>

				<RowDefinition Height="Auto"></RowDefinition>
				<RowDefinition Height="Auto"></RowDefinition>
				<RowDefinition Height="Auto"></RowDefinition>

				<RowDefinition Height="Auto"></RowDefinition>
				<RowDefinition></RowDefinition>

				<RowDefinition Height="Auto"></RowDefinition>
				<RowDefinition></RowDefinition>
			</Grid.RowDefinitions>

			<Label Grid.Row="0">Информация</Label>
			<TextBlock Grid.Row="1" x:Name="CurrentPrice_SupplierFullName" Style="{StaticResource PriceInfo}"></TextBlock>

			<TextBlock Grid.Row="2" Style="{StaticResource PriceInfo}">
				Телефон: <Run x:Name="CurrentPrice_Phone"></Run>
			</TextBlock>

			<TextBlock Grid.Row="3" Style="{StaticResource PriceInfo}">
				Минимальный заказ: <Run x:Name="CurrentPrice_MinReq"></Run>
			</TextBlock>

			<TextBlock Grid.Row="4" Style="{StaticResource PriceInfo}">
				Email: <Run x:Name="CurrentPrice_Email"></Run>
			</TextBlock>

			<Label Grid.Row="5">Контактная информация:</Label>
			<ScrollViewer Grid.Row="6" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<TextBlock Padding="5" TextWrapping="Wrap" x:Name="CurrentPrice_ContactInfo"></TextBlock>
			</ScrollViewer>

			<Label Grid.Row="7">Оперативная информация:</Label>
			<ScrollViewer Grid.Row="8" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<TextBlock Padding="5" TextWrapping="Wrap" x:Name="CurrentPrice_OperativeInfo"></TextBlock>
			</ScrollViewer>
		</Grid>

		<StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource ResourceKey=FilterPanel}">
			<CheckBox Style="{StaticResource ResourceKey=FilterCheckBox}" Name="ShowLeaders">Показать только лидирующие позиции (F3)</CheckBox>
		</StackPanel>
	</Grid>
</UserControl>
