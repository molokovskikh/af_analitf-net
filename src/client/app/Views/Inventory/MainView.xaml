<UserControl x:Class="AnalitF.Net.Client.Views.Inventory.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AnalitF.Net.Client.Views.Inventory"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<ToolBar Grid.Row="0">
			<Menu>
				<MenuItem Header="Документы">
					<MenuItem Header="Приход от поставщика" Name="ReceivingOrders" />
          <MenuItem Header="Чеки" Name="Checks" />
        </MenuItem>
				<MenuItem Header="Отчеты">
					<MenuItem Header="Товарные запасы" Name="Stocks" />
				</MenuItem>
			</Menu>
		</ToolBar>
		<ContentControl Grid.Row="1" Name="ActiveItem" />
	</Grid>
</UserControl>
