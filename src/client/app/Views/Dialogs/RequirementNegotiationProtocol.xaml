<UserControl x:Class="AnalitF.Net.Client.Views.Dialogs.RequirementNegotiationProtocol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AnalitF.Net.Client.Views.Dialogs"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Vertical" Grid.Row="0">
      <Label Content="ФИО уполномоченного лица поставщика:" />
      <TextBox Name="Fio"/>
    </StackPanel>
    <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin =" 1,5,0,1" HorizontalAlignment="Center">
      <Button Name="OK" Margin="0 0 5 0">ОК</Button>
      <Button Name="Close">Отмена</Button>
    </StackPanel>

  </Grid>
</UserControl>
