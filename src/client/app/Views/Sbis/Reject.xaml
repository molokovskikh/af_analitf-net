<UserControl x:Class="AnalitF.Net.Client.Views.Sbis.Reject"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	MinHeight="300" MinWidth="300">
	<Grid Margin="3">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Label Grid.Row="0" FontSize="18">Отклонение документа</Label>
		<Label Grid.Row="2">Причина</Label>
		<TextBox Grid.Row="3" Name="Comment" />

		<StackPanel Orientation="Horizontal" Grid.Row="4">
			<Button Margin="3" Name="Save">Отклонить</Button>
			<Button Margin="3" Name="TryClose">Отмена</Button>
		</StackPanel>
	</Grid>
</UserControl>
