<UserControl x:Class="AnalitF.Net.Client.Views.Diadok.Revocation"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	IsEnabled="{Binding IsEnabled.Value}"
	MinHeight="300" MinWidth="300">
		<Grid Margin="3">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<Label Grid.Row="0" FontSize="18">Аннулирование документа</Label>
			<Label Grid.Row="2">Комментарий к аннлированию</Label>
			<TextBox Grid.Row="3" />

			<StackPanel Orientation="Horizontal" Grid.Row="4">
				<Button Margin="3" Name="Save">Подписать и отправить контрагенту</Button>
				<Button Margin="3" Name="TryClose">Отмена</Button>
			</StackPanel>
		</Grid>
</UserControl>
