<UserControl x:Class="AnalitF.Net.Client.Views.Diadok.Resolution"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	IsEnabled="{Binding IsEnabled.Value}"
	MinHeight="300" MinWidth="300">
		<Grid Margin="3">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<Label Grid.Row="0" FontSize="18" Name="Header" />
			<Label Grid.Row="1" Name="Clarification" />
			<Label Grid.Row="2">Комментарий</Label>
			<TextBox Grid.Row="3" Name="Comment"/>

    <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="0,10,0,7">
				<Button Name="Save" Margin="0,0,10,0" Height="30" Content="{Binding ActionName.Value}" />
				<Button Name="TryClose" Width="90">Отмена</Button>
			</StackPanel>
		</Grid>
</UserControl>
